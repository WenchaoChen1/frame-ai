# 开发环境 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制依赖文件
COPY package.json package-lock.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 环境变量（运行时）
ENV VITE_PORT=9101
ENV VITE_HOST=0.0.0.0

# 暴露 Vite 开发服务器端口
EXPOSE 9101

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

